{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Local studio window; process and file operations stay in typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
